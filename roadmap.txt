

	pour chaque demande de verification :
		- selectionner les salles avec les capacités suffisantes --> un esemble
		- dans cet ensemble, selectionner les salles disponibles dans ce créneau horaire : --> un sous-ensemble1
		- dans ee sous-ensemble1 de salle : pour chaque salle, ajouter les éléments manquants si disponibles : sous-ensemble2
		- selectionner le premier élément du sous-ensemble2 si non-vide, sinon pas de disponibilité.
		
		
		ensemble : select * from room where room.sanitary_capacity >= meeting.attendantsNumber and ;
		
		sous-ensemble1 : DateRangeComparator dateRangeComparator = new DateRangeComparator(meeting.startTime, meeting.endTime);
						for(Room room: roomList) {
							if(dateRangeComparator.isWithinRange(room.useStart) || dateRangeComparator.isWithinRange(room.useEnd)) {
								roomList.remove(room);
							}
						}
						
		sous-ensemble2 : 